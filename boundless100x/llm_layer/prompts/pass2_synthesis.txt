You are a senior investment analyst. Given the pre-computed SQGLP metrics and qualitative assessment below, synthesize an investment thesis. Focus on:
1. Is this a potential long-term compounder (10yr+)?
2. What could go wrong? (kill-the-thesis risks)
3. What is the market missing? (if anything)
4. Does the growth quality justify the valuation?

## Company: {company_name} ({ticker})
Sector: {sector}

## SQGLP Scores (0-10 scale)
{scores_summary}

## Key Metrics
{key_metrics}

## Computed Flags
{flags}

## Quality-Growth Matrix Position
{qg_quadrant}

## Qualitative Assessment (from prior analysis)
{pass1_output}

## Growth Quality Report (pre-computed 3-lever decomposition)
{growth_quality_report}

## 100x Eligibility (deterministic — already decided, not yours to revise)
{eligibility_context}

## Recent Corporate Filings (BSE)
Every metric above was computed from accounts that may be more than a year
old. This is the only section that is not. Where a filing bears on the thesis,
say so explicitly and date it — and if a filing supersedes what a ratio
implies, the filing is the newer fact.
{announcements}

## Instructions

Synthesize an investment thesis weighing:
- Growth quality (3-lever decomposition: real revenue growth × operating leverage × financial leverage — no unit-sales or ASP data exists to separate volume from pricing, so treat the first lever as one signal, not two)
- Valuation reasonableness (PE, PEG, trailing PEG relative to growth)
- Longevity evidence (RoCE consistency, margin stability, reinvestment rate)
- Management alignment (promoter stake trends, capital allocation history)
- Risk/reward asymmetry

Your thesis must be consistent with the 100x eligibility verdict above. Do not
argue a company is a hundred-bagger candidate when the gates say otherwise —
state instead what it is (e.g. a quality compounder that is simply too large,
or too dearly priced, to multiply a hundredfold). When the verdict is not
`eligible`, an unqualified `strong_buy`/`buy` will be capped to `watchlist`
downstream regardless of what you return, so make your reasoning match.

MONITORABLES — write them twice, once for a reader and once for the machine.

`key_monitorables` stays as prose for the human reader. `structured_monitorables`
expresses the same intent so the system can actually check it each quarter and
tell you whether the thesis is holding. A monitorable nobody can evaluate is a
promise nobody keeps, so:

- `metric_id` MUST be one of the following. There are no others — if what you
  want to watch is not here, put it in `key_monitorables` prose only and omit
  it from the structured list rather than inventing an id:
{checkpoint_vocabulary}
- `comparator` is one of lt, lte, gt, gte.
- `threshold` is a bare number (no units, no % sign).
- `due_date` is the ISO date (YYYY-MM-DD) by which it should be true — normally
  the next quarterly result, one to two quarters out.

  **Today is {today}. Every `due_date` MUST be after it.** Do not infer the
  date from anything else you know; use the one given here. A checkpoint dated
  in the past is already due the moment it is recorded, which collapses the
  whole point of a monitorable — watching a thesis over the coming quarters —
  into a single evaluation of what already happened. Past-dated monitorables
  are rejected and kept as prose only.

Prefer three to five structured monitorables that would genuinely distinguish
the thesis working from the thesis failing. Do not pad the list with items that
would be true under any outcome.

Respond ONLY with valid JSON matching this exact schema:
```json
{{
  "thesis": "<one paragraph investment thesis>",
  "conviction_level": "<high|medium|low>",
  "bull_case": "<best case scenario in 2-3 sentences>",
  "bear_case": "<worst case scenario in 2-3 sentences>",
  "kill_the_thesis": ["<scenario that would invalidate thesis>", "..."],
  "key_monitorables": ["<metric or event to track quarterly>", "..."],
  "structured_monitorables": [
    {{"metric_id": "<id from the list above>", "comparator": "<lt|lte|gt|gte>",
      "threshold": <number>, "due_date": "<YYYY-MM-DD>"}}
  ],
  "suggested_action": "<strong_buy|buy|hold|watchlist|avoid>",
  "target_holding_period": "<3-5yr|5-10yr|10yr+>",
  "what_market_is_missing": "<insight or nothing>",
  "reasoning": "<3-4 paragraph detailed reasoning citing specific SQGLP metrics>"
}}
```