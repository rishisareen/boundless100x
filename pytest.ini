[pytest]
testpaths = tests
markers =
    network: hits a live third-party service and writes into raw_data/
# The live Screener.in scrape is opt-in: run it with `-m network`.
addopts = -m "not network"
